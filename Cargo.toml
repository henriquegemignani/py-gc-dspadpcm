[package]
name = "gc-dspadpcm"
version = "0.1.0"
edition = "2021"

[lib]
name = "gc_dspadpcm"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.28", features = ["extension-module", "abi3-py310"] }

[profile.release]
lto = true
codegen-units = 1
