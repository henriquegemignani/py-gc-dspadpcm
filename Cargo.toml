[package]
name = "gc-dspadpcm"
version = "0.1.0"
edition = "2021"

[lib]
name = "gc_dspadpcm"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.21", features = ["extension-module"] }

[profile.release]
lto = true
codegen-units = 1
